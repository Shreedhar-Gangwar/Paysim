# Copy this file to .env and fill in your local PostgreSQL credentials.
# .env is git-ignored; credentials never live in scripts or SQL.
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=paysim
