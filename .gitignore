# Raw data — too large for git; obtain from Kaggle (see README)
data/raw/

# Local environment / credentials — never commit
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Power BI working files. The .pbix may be committed if you want it in the repo,
# but its temp/cache siblings never should be.
*.pbix.tmp
*.pbix.bak
~$*.pbix

# OS / editor noise
Thumbs.db
.DS_Store
